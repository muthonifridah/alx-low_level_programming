#include "main.h"

/**
 * mul - multiples two integers
 * @x: The first number to be checked
 * @y: The second number to be checked
 * Return: The result from multiplication between x and y
 */
int mul(int x, int y)
{
	return (x * y);
}
